import VideoEmbed from '../../components/content/VideoEmbed';
import Callout from '../../components/content/Callout';
import KeyConcepts from '../../components/content/KeyConcepts';
import CodeEditor from '../../components/exercises/CodeEditor';
import Quiz from '../../components/exercises/Quiz';
import ProTerminal from '../../components/content/ProTerminal';
import Infographic from '../../components/content/Infographic';
import ComparisonTable from '../../components/content/ComparisonTable';
import StackVisualizer from '../../components/content/StackVisualizer';
import MicroRegisters from '../../components/content/MicroRegisters';

# Lezione 2: Variabili, Funzioni e Memoria

Benvenuti alla seconda lezione! Oggi andremo a fondo nella struttura di un programma C, capendo finalmente cosa succede "sotto il cofano" quando dichiariamo una variabile o chiamiamo una funzione.

<VideoEmbed videoId="Z84vlG1RRtg" title="Let's learn C: lezione 2" />

## Struttura del Programma e `#include`

Ogni programma C inizia spesso con delle direttive al preprocessore. La più comune è `#include <stdio.h>`.

</Callout>

### Il Compilatore ti aiuta: -Wall e -Wextra

Dato che il C ti lascia molta libertà (anche quella di sbagliare), è facile commettere errori logici che non bloccano la compilazione ma causano bug a runtime. Per questo motivo, Salvatore consiglia di usare sempre dei **flag** (opzioni) durante la compilazione per attivare gli avvisi:

- `-Wall`: Sta per **Warn All**. Nonostante il nome, abilita una vasta collezione di avvisi *comuni* e fondamentali.
- `-Wextra`: Abilita ancora più avvisi per casi particolari e sottili che `-Wall` non copre.

<Callout type="pro" title="Massima Sicurezza">
Salvatore li usa spesso entrambi: `gcc -Wall -Wextra file.c`. Se il compilatore "si lamenta", non ignorarlo: ti sta segnalando un potenziale bug prima ancora che tu esegua il programma.
</Callout>


### La Funzione `main` e i Vincoli del C
La funzione `main` è il cuore del programma. È il punto di ingresso (entry point) da cui il sistema operativo inizia l'esecuzione. In C, **tutto il codice eseguibile deve trovarsi all'interno di una funzione**. 

<Callout type="warning" title="Niente Codice 'Volante'">
A differenza di Python o Bash, non puoi scrivere una `printf` direttamente all'inizio del file. Se provi a farlo, il compilatore ti darà un errore del tipo `expected declaration or statement`.
</Callout>

```c
int main(void) {
    // Il tuo codice deve stare qui dentro!
    return 0;
}
```

- `int`: Indica che la funzione restituisce un numero intero al sistema operativo (0 significa successo).
- `void`: Indica esplicitamente che la funzione non accetta argomenti in ingresso.

### Un Esempio Completo: La funzione `sum`

Ecco il primo vero programma che Salvatore ci ha mostrato. Combina inclusione, definizione di una funzione e output:

```c
#include <stdio.h>

// Definizione di una funzione che somma due interi
int sum(int a, int b) {
    return a + b;
}

int main(void) {
    // Chiamiamo la funzione sum e stampiamo il risultato
    printf("Hello World %d\n", sum(10, 20));
    return 0;
}
```

Eseguendo questo codice, otterrai:
`Hello World 30`

---

## printf: La Magia della Formattazione

Il nome `printf` sta per **Print Formatted**. È una funzione estremamente potente perché accetta un **numero variabile di argomenti** (tecnicamente detta funzione variadica).

### Stampare Valori con i Segnaposto

Per stampare il risultato di una funzione o il valore di una variabile, dobbiamo dire alla `printf` che tipo di dato si aspetta usando dei **segnaposto** (format specifier) che iniziano con il simbolo `%`.

Nell'esempio della funzione `sum`, abbiamo usato `%d`:

```c
printf("Hello World %d\n", sum(10, 20));
```

- `%d`: Sta per **Decimal**. Dice alla `printf` di aspettarsi un numero intero.
- `\n`: Rappresenta il carattere di "nuova riga".

### Più Parametri, Più Potere

Possiamo passare molti parametri alla stessa `printf`. L'importante è che l'ordine dei segnaposto corrisponda esattamente all'ordine delle variabili passate dopo la stringa di formato.

```c
printf("Ciao %s, il risultato è %d\n", "User", 30);
//      [FORMAT]           [ARG 1]     [ARG 2]
```

<ComparisonTable 
  title="Corrispondenza Parametri"
  headers={["Segnaposto", "Tipo di Dato", "Esempio Argomento"]}
  rows={[
    ["%d", "Intero (int)", "30, -5, sum(10, 20)"],
    ["%s", "Stringa (testo)", "\"Ciao\", \"User\""],
    ["%f", "Numero con virgola (float)", "3.14"]
  ]}
/>

<Callout type="danger" title="Attenzione: Undefined Behavior">
  Se dici alla `printf` che stamperai due interi (`%d %d`) ma gliene passi solo uno, il C non si fermerà! Leggerà "immondizia" dalla memoria o andrà in crash. Questo è il primo assaggio della cosiddetta **Undefined Behavior** (Comportamento Indefinito).
</Callout>


### Il Concetto di Espressione

Perché possiamo scrivere `sum(10, 20)` dentro la `printf`? La risposta sta nel **concetto di espressione**.

In programmazione, un'espressione è un pezzo di codice che, una volta eseguito dal computer, "diventa un valore". Questo processo si chiama **valutazione**.

- `10 + 20` è un'espressione che viene valutata come `30`.
- `sum(10, 20)` è un'espressione che "chiama" la funzione, la esegue e alla fine viene sostituita dal suo risultato (il valore di `return`), diventando `30`.

Quando la `printf` viene eseguita, non vede più la chiamata alla funzione, ma riceve direttamente il numero `30`.

---

## Funzioni: Prototipo vs Definizione

Salvatore ci ha mostrato come definire una funzione per sommare due numeri. È importante distinguere tra come la funzione viene "annunciata" e come viene "scritta".

<ComparisonTable 
  title="Anatomia di una Funzione"
  headers={["Elemento", "Esempio", "Descrizione"]}
  rows={[
    ["Tipo Ritorno", "int", "Il tipo di dato che la funzione 'restituisce'."],
    ["Nome", "sum", "L'identificatore usato per chiamare la funzione."],
    ["Parametri", "(int a, int b)", "I dati che la funzione riceve in input."],
    ["Corpo", "{ return a + b; }", "Le istruzioni racchiuse tra parentesi graffe."]
  ]}
/>

---

## Variabili e Tipi Espliciti

In C, a differenza di linguaggi come Python o JavaScript, devi **dichiarare esplicitamente** il tipo di ogni variabile. Il compilatore non fa "inferenza" automatica.

```c
int a = 10;   // Dichiarazione e Inizializzazione (assegna 10 durante la creazione)
int b;        // Sola Dichiarazione (contiene "spazzatura" della RAM)
b = 20;       // Assegnazione (sovrascrive il valore precedente)
int c = a + b; // Inizializzazione tramite espressione
```

<ComparisonTable 
  title="Dichiarazione vs Inizializzazione"
  headers={["Operazione", "Esempio", "Effetto in Memoria"]}
  rows={[
    ["Dichiarazione", "<code>int x;</code>", "Prenota lo spazio, ma il contenuto è casuale."],
    ["Inizializzazione", "<code>int x = 10;</code>", "Prenota lo spazio e scrive subito il valore."],
    ["Assegnazione", "<code>x = 20;</code>", "Cambia il valore di uno spazio già esistente."]
  ]}
/>

<Callout type="pro" title="Expert Mindset">
Specificare il tipo (es. `int`) permette al compilatore di sapere esattamente quanti byte riservare in memoria e come interpretarli. Questo è il segreto delle performance del C!
</Callout>

---

## Il "Ciclo di Vita" nella Memoria (Lo Stack)

Cosa succede quando chiami una funzione? Dove finiscono le variabili locali?
Le variabili definite dentro una funzione vivono in una zona di memoria chiamata **Stack** (Pila).

<StackVisualizer />

Quando una funzione viene chiamata, viene creato un **Stack Frame**. 

<Callout type="info" title="Creazione e Distruzione">
  Le variabili locali (inclusi gli argomenti della funzione) vengono create "al volo" ogni volta che chiami la funzione. Quando la funzione ritorna con `return`, lo spazio viene liberato. 
</Callout>

<Callout type="warning" title="The Ghost in the Stack">
  **Attenzione**: "liberato" non significa "cancellato"! In C, per motivi di performance, la memoria non viene azzerata quando una funzione termina. I dati rimangono lì finché un'altra funzione non sovrascrive quel pezzetto di Stack. Se dichiari `int x;` senza inizializzarla, leggerai i "fantasmi" di chi ha usato quel posto prima di te.
</Callout>

Quando la funzione termina (`return`), il suo frame viene "distrutto" e la memoria liberata.

---

## Registri e Microprocessore (Appendice)

In questa appendice, Salvatore ci porta indietro nel tempo con il microprocessore **6502** per spiegarci la differenza tra registri e memoria RAM.

<VideoEmbed videoId="r6mU_IHXEps" title="Appendice Lezione 2: La vita delle variabili locali" />

<Callout type="pro" title="Memory Mapped I/O">
  <MicroRegisters />
  Nello schermo qui sopra, stiamo scrivendo direttamente in memoria RAM all'indirizzo `$0200`. Il microprocessore non "sa" di stare disegnando: sta solo muovendo byte. 
  
  In assembly 6502, per accendere un pixel potremmo scrivere:
  ```assembly
  LDA #$01    ; Carica il colore 1 (es. Bianco) nel registro Accumulatore
  STA $0200   ; Memorizza (scrivi) il valore all'indirizzo $0200
  ```
  
  È l'hardware video che interpreta quei byte come colori di pixel. In C, noi scriviamo astrattamente `a = 10;`, ma "sotto il cofano" il compilatore genera istruzioni simili a queste per muovere i dati tra i **Registri** della CPU e la **RAM**. Questa è la base dei sistemi embedded e delle vecchie console.
</Callout>

## Il valore di ritorno di `main`

Perché `main` ritorna un `int`? Serve a comunicare al **Sistema Operativo** se il programma è terminato correttamente.
- `return 0;` significa "Tutto ok".
- `return 1;` (o altro) segnala un errore.

<Callout type="tip">
Su Linux, puoi vedere il valore di ritorno dell'ultimo comando digitando `echo $?` nel terminale.
</Callout>

### Codici di Ritorno e Logica Shell
Il valore di ritorno di `main` non è solo una formalità. In sistemi Unix, permette di concatenare programmi in base al loro successo.
- **AND Logico (`&&`)**: `comando1 && comando2` -> `comando2` viene eseguito solo se `comando1` ritorna **0** (successo).
- **OR Logico (`||`)**: `comando1 || comando2` -> `comando2` viene eseguito solo se `comando1` fallisce (diverso da 0).

Questo meccanismo di "short-circuit" è fondamentale per automatizzare task complessi dove ogni passo dipende dal precedente.

<Callout type="tip">
I **Registri** sono piccole "variabili" dentro la CPU, velocissime. La **RAM** è più grande ma più lenta. Il compilatore C cerca di usare i registri il più possibile per velocizzare il tuo codice.
</Callout>

---

## Esercizi di Verifica

<Quiz 
  title="Quiz Standard" 
  questions={[
    {
      id: 1,
      question: "Cosa significa il `'void'` tra le parentesi di `int main(void)`?",
      options: [
        "Che la funzione non restituisce nulla",
        "Che la funzione non accetta parametri",
        "Che il programma è vuoto",
        "Che la funzione è un errore"
      ],
      correctAnswer: 1,
      explanation: "L'uso di `'void'` indica esplicitamente che la funzione `main` non riceve argomenti."
    },
    {
      id: 2,
      question: "Dove vengono memorizzate le variabili locali di una funzione?",
      options: [
        "Nell'Hard Disk",
        "Nella memoria Video",
        "Nello Stack (Pila)",
        "Nei Registri Globali"
      ],
      correctAnswer: 2,
      explanation: "Le variabili locali vivono nello Stack e vengono rimosse quando la funzione termina."
    },
    {
      id: 3,
      question: "Quale flag del compilatore abilita tutti i warning comuni?",
      options: [
        "`-Werror`",
        "`-O3`",
        "`-Wall`",
        "`-all`"
      ],
      correctAnswer: 2,
      explanation: "`-Wall` (Warn All) è fondamentale per individuare potenziali bug prima di eseguire il programma."
    },
    {
      id: 4,
      question: "Qual è la differenza principale tra un Registro e la RAM?",
      options: [
        "I registri sono più lenti",
        "La RAM è dentro la CPU",
        "I registri sono vicini alla CPU e velocissimi, la RAM è esterna e più capiente",
        "Non c'è differenza"
      ],
      correctAnswer: 2,
      explanation: "I registri sono piccole unità di memoria interne alla CPU, usate per operazioni immediate. La RAM è la memoria principale del sistema."
    },
    {
      id: 5,
      question: "Perché inizializziamo le variabili (es. `int a = 0;`)?",
      options: [
        "Perché il C lo richiede sempre",
        "Per evitare che la variabile contenga 'immondizia' (vecchi valori rimasti nello stack)",
        "Per rendere il codice più lungo",
        "Per aiutare il linker"
      ],
      correctAnswer: 1,
      explanation: "In C, le variabili locali non inizializzate contengono valori casuali (garbage) rimasti in quella posizione di memoria dello stack."
    }
  ]}
/>

---

<KeyConcepts 
  title="In Breve"
  concepts={[
    {
        title: "Tipizzazione Esplicita",
        description: "In C ogni variabile ha un tipo fisso. Questo garantisce sicurezza e controllo totale sulla memoria."
    },

    {
        title: "Stack Frames",
        description: "La memoria delle funzioni è organizzata come una pila: l'ultima chiamata è la prima ad essere rimossa."
    },
    {
        title: "Compiler Warnings",
        description: "Usa sempre -Wall. Un compilatore che 'si lamenta' è un compilatore che ti sta aiutando."
    },
    {
        title: "Registri vs RAM",
        description: "I registri sono interni alla CPU e velocissimi; la RAM è esterna e più capiente."
    }
  ]}
/>

### Pro Challenge
<Quiz 
  title="Pro Challenge: Final Quiz"
  questions={[
    {
      id: 1,
      question: "Perché una variabile locale non inizializzata (es. `int x;`) è considerata pericolosa anche se il programma sembra funzionare inizialmente?",
      options: [
        "Perché occupa più RAM",
        "Perché contiene 'spazzatura', ovvero il valore rimasuglio di chi ha usato quel pezzetto di Stack precedentemente (Stack Reuse)",
        "Perché il compilatore cancella il file se la trova",
        "Perché rende il computer più caldo"
      ],
      correctAnswer: 1,
      explanation: "Lo Stack è una zona di memoria che viene continuamente riutilizzata. Quando una funzione finisce, il suo spazio viene liberato ma i dati non vengono cancellati (per velocità). La funzione successiva che usa quello spazio troverà i 'fantasmi' della funzione precedente."
    },
    {
      id: 2,
      question: "Cosa accade fisicamente quando chiamiamo una funzione in C?",
      options: [
        "Il codice della funzione viene copiato nel punto di chiamata",
        "Viene creato un nuovo 'Stack Frame' che contiene i parametri e le variabili locali della funzione",
        "La CPU smette di funzionare finché la funzione non termina",
        "Le variabili vengono salvate sul disco fisso"
      ],
      correctAnswer: 1,
      explanation: "La chiamata a funzione crea una struttura nello Stack chiamata 'Frame'. È come un cassetto temporaneo dove la funzione mette tutto ciò che le serve. Quando la funzione 'ritorna', il cassetto viene svuotato (o meglio, rimosso dalla pila)."
    },
    {
      id: 3,
      question: "Perché a volte usiamo la parola chiave `volatile` davanti a una variabile?",
      options: [
        "Per renderla più veloce",
        "Per dire al compilatore di non ottimizzarla (es. non metterla in un registro), perché il suo valore potrebbe cambiare inaspettatamente (es. hardware esterno)",
        "Per cancellarla dalla memoria dopo l'uso",
        "Per proteggerla dai virus"
      ],
      correctAnswer: 1,
      explanation: "Il compilatore C è molto aggressivo. Se vede che leggi una variabile due volte senza scriverci nulla in mezzo, potrebbe pensare che il valore sia lo stesso e non rileggerlo dalla RAM. `volatile` forza il compilatore a rileggere il valore ogni singola volta, essenziale per interagire con l'hardware o con i thread."
    }

  ]}
/>

### Terminal Challenge: Investigating Memory 

<ProTerminal 
  challenges={[
    {
        id: "xxd-dump",
        simulatedSuccessOutput: `00000000: 7f45 4c46 0201 0100 0000 0000 0000 0000  .ELF............\n00000010: 0300 3e00 0100 0000 4010 0000 0000 0000  ..>.....@.......`,
        title: "Vedere l'Invisibile",
        description: "Salvatore ha menzionato uno strumento per guardare 'dentro' gli eseguibili. Quale comando useresti per vedere il contenuto esadecimale di `a.out`?",
        commands: [],
        expectedOutput: "",
        hints: [
            "Inizia con 'x' e fa il dump esadecimale"
        ],
        explanation: "Ottimo! `xxd` è fondamentale per vedere i byte reali di un binario.",
        xpReward: 150,
        expectedCommand: "xxd a.out"
    },
    {
        id: "nm-symbols",
        simulatedSuccessOutput: `0000000000001130 T main\n                 U puts@GLIBC_2.2.5\n0000000000001000 t _init`,
        title: "La Lista dei Nomi",
        description: "Ogni funzione diventa un 'simbolo'. Quale comando elenca tutti i simboli (nomi di funzioni e variabili) in `a.out`?",
        commands: [],
        expectedOutput: "",
        hints: [
            "Comando di due lettere, abbreviazione di 'names'"
        ],
        explanation: "Esatto! `nm` ti permette di vedere quali funzioni sono definite nel tuo codice.",
        xpReward: 200,
        expectedCommand: "nm a.out"
    },
    {
        id: "objdump-header",
        simulatedSuccessOutput: `a.out:     file format elf64-x86-64\narchitecture: i386:x86-64, flags 0x00000150:\nHAS_SYMS, DYNAMIC, D_PAGED\nstart address 0x0000000000001040`,
        title: "L'Intestazione",
        description: "Quale comando useresti per visualizzare l'header dell'eseguibile e trovarne il punto di ingresso (entry point)?",
        commands: [],
        expectedOutput: "",
        hints: [
            "Inizia con 'obj'"
        ],
        explanation: "Perfetto! `objdump -f` mostra i metadati vitali del binario.",
        xpReward: 250,
        expectedCommand: "objdump -f a.out"
    }
]}
/>

Ci vediamo alla prossima lezione!


