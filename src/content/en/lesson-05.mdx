import VideoEmbed from '../../components/content/VideoEmbed';
import Callout from '../../components/content/Callout';
import KeyConcepts from '../../components/content/KeyConcepts';
import ASCIIConverter from '../../components/content/ASCIIConverter';
import Infographic from '../../components/content/Infographic';
import CodeEditor from '../../components/exercises/CodeEditor';
import Quiz from '../../components/exercises/Quiz';

# Lesson 5: Strings & Characters (ASCII)

In the last lesson, we looked at bits and numbers. But how do we represent text? In C, the answer is simple: characters **are** numbers.

<VideoEmbed videoId="SWWHqgSwQFw" title="Lesson 5: Strings and Characters" />

<KeyConcepts 
  concepts={[
    {
      title: "The char type",
      description: "An 8-bit integer that can hold a value from 0 to 255. In C, it's written inside single quotes, e.g., 'A'."
    },
    {
      title: "ASCII Table",
      description: "A standard that maps every number to a specific character. For example, 65 corresponds to 'A'."
    },
    {
      title: "Format Strings",
      description: "Placeholders like %d for numbers and %c for characters that we use in printf."
    }
  ]} 
/>

## The Truth Behind Characters

When you write `char c = 'A';`, the computer doesn't save the letter "A", but the number **65**. This is the core of the ASCII system.

<ASCIIConverter />

<Infographic 
  src="/images/ascii_visual.png" 
  alt="ASCII conversion diagram" 
  caption="From the letter to the number, down to the bits in the processor."
/>

<Callout type="info" title="Quotes vs Apostrophes">
In C, there's a big difference:
- `'A'` (single quotes) is a `char` type (a single number).
- `"A"` (double quotes) is a string (a zero-terminated array of characters).
</Callout>

## Type Promotion

Did you know that if you pass a `char` to a function with variable arguments (like `printf`), C automatically promotes it to an `int`? This is why `%d` works for printing the numeric code of a character too!

<CodeEditor 
  initialCode={`#include <stdio.h>\n\nint main() {\n    char letter = 'A';\n    printf("Character: %c\\n", letter);\n    printf("ASCII Code: %d\\n", letter);\n    return 0;\n}`}
  language="c"
/>

## Review Quiz

<Quiz 
  questions={[
    {
      question: "What is the fundamental difference between 'X' and \"X\" in C?",
      options: [
        "None, they are interchangeable",
        "'X' is a single character (number), \"X\" is a string (array)",
        "'X' is faster than \"X\"",
        "\"X\" is only used for Python"
      ],
      correctAnswer: 1,
      explanation: "Single quotes indicate a single numeric ASCII value, while double quotes indicate an array of characters in memory."
    },
    {
      question: "What happens if you print a char with %d?",
      options: [
        "The program crashes",
        "The character is printed",
        "Its numeric ASCII code is printed",
        "A format error is printed"
      ],
      correctAnswer: 2,
      explanation: "Since characters are represented as numbers, %d prints the corresponding integer value in the ASCII table."
    },
    {
      question: "Which number does the uppercase 'A' usually correspond to?",
      options: ["1", "32", "65", "97"],
      correctAnswer: 2,
      explanation: "65 is the standard code for uppercase 'A'. Lowercase 'a' is 97."
    },
    {
      question: "What is the primary difference between ASCII and UTF-8?",
      options: [
        "None, they are the same thing",
        "ASCII uses 7 bits (128 characters), UTF-8 is a modern system that can use multiple bytes to represent emojis and characters from every language",
        "ASCII is slower",
        "UTF-8 does not support numbers"
      ],
      correctAnswer: 1,
      explanation: "ASCII is the base set of 128 characters. UTF-8 is backward compatible with ASCII but expands to cover the entire Unicode universe."
    },
    {
      question: "Which standard library should you include to use functions like isdigit() or toupper()?",
      options: ["stdio.h", "ctype.h", "string.h", "math.h"],
      correctAnswer: 1,
      explanation: "ctype.h (Character Type) contains macros and functions for testing and converting individual characters."
    }
  ]}
/>

---

<KeyConcepts title="Hacker Key Concepts">
  - **Characters are numbers**: Every character (`char`) is actually an 8-bit integer.
  - **ASCII Table**: The universal map that assigns a unique number to every text symbol.
  - **Apostrophes vs Quotes**: `'A'` is a character (a number), `"A"` is a string (a more complex container).
</KeyConcepts>

### Hacker Challenge 🏆

<Quiz 
  title="Hacker Challenge: Final Quiz"
  questions={[
    {
      id: 1,
      question: "If you have `char c = 'A';` and you write `c = c + 32;`, what will `printf(\"%c\", c)` print?",
      options: [
        "An error, you cannot add numbers to characters",
        "'a' (the lowercase version)",
        "The character '32'",
        "A random symbol"
      ],
      correctAnswer: 1,
      explanation: "In the ASCII table, the distance between an uppercase letter and its lowercase version is exactly 32. Adding 32 to 'A' (65) takes us to 'a' (97). This demonstrates that in C, characters are essentially numbers that we can perform arithmetic on."
    },
    {
      id: 2,
      question: "What is the difference between the character '0' and the integer value 0?",
      options: [
        "None, they are the same value in memory",
        "The integer value 0 is the string terminator (NULL), while the character '0' has ASCII value 48",
        "'0' takes up more memory than 0",
        "The character '0' is only used in mathematics"
      ],
      correctAnswer: 1,
      explanation: "This is fundamental: `0` (number) is the null byte, used to tell C 'here the string ends'. `'0'` (character) is the key on the keyboard which, in the ASCII table, corresponds to the number 48."
    },
    {
      id: 3,
      question: "What does the `\\b` escape sequence do when printed?",
      options: [
        "Inserts a blank line",
        "Performs a 'backspace', ideally deleting the previous character by moving the cursor left",
        "Turns off the monitor",
        "Creates an acoustic signal"
      ],
      correctAnswer: 1,
      explanation: "Backspace! It is one of the ASCII control characters that allows you to manipulate the cursor position in the terminal."
    }
  ]}
/>

### Terminal Challenge: Manual Hunting 📖

<HackerTerminal 
  title="Hacker Challenge: Level 5 - Task 1"
  description="We learned how to use output functions. What is the terminal command to read the 'manual' of a function like printf to discover all its secrets?"
  command="google printf help"
  expectedOutput="man printf"
  inputLabel="Complete Command:"
  placeholder="e.g. man function_name"
  hint="It's short for 'manual'."
  explanation="Exactly! `man` is a C programmer's best friend. It gives you the official, detailed, and precise documentation for every library function and system command."
  xpReward={150}
/>

<HackerTerminal 
  title="Hacker Challenge: Level 5 - Task 2"
  description="In C you can convert a character from lowercase to uppercase by subtracting 32. There is a shell command that can do the same for an entire file or input. Which one would you use to transform 'hi' into 'HI'?"
  command="echo 'hi' | ..."
  expectedOutput="tr 'a-z' 'A-Z'"
  inputLabel="Command (tr):"
  placeholder="e.g. tr 'set1' 'set2'"
  hint="The command stands for 'translate' or 'transliterate'."
  explanation="Great! `tr` is very powerful for manipulating utility character streams, replicating the conversion logic we would do in C but directly on the command line."
  xpReward={200}
/>

<HackerTerminal 
  title="Hacker Challenge: Level 5 - Task 3"
  description="You want to see the hexadecimal value of every single character contained in a 'text.txt' file. Which command would you use to perform a byte-by-byte hexadecimal dump?"
  command="cat text.txt"
  expectedOutput="od -A n -t x1 text.txt"
  inputLabel="Command (od):"
  placeholder="e.g. od -options filename"
  hint="The command stands for 'Octal Dump', but with the -t x1 option it shows hexadecimals."
  explanation="Excellent! `od` allows you to see the 'naked flesh' of the file, showing ASCII codes in hexadecimal, decimal, or octal format. Indispensable for debugging corrupt files."
  xpReward={250}
/>

See you in the next lesson!
