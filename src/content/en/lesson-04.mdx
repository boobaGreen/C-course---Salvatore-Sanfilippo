import VideoEmbed from '../../components/content/VideoEmbed';
import Callout from '../../components/content/Callout';
import KeyConcepts from '../../components/content/KeyConcepts';
import LimitsExplorer from '../../components/content/LimitsExplorer';
import BinaryVisualizer from '../../components/content/BinaryVisualizer';
import Infographic from '../../components/content/Infographic';
import CodeEditor from '../../components/exercises/CodeEditor';
import Quiz from '../../components/exercises/Quiz';

# Lesson 4: Integers, Memory & Limits

Welcome to lesson four! Today we'll dig beneath the surface of C to understand how numbers are actually represented in computer memory and why sizes matter.

<VideoEmbed videoId="YNsXyasn4R4" title="Lesson 4: Integer Types and Memory" />

<KeyConcepts 
  concepts={[
    {
      title: "sizeof",
      description: "The fundamental operator to discover how many bytes a type occupies on your specific machine."
    },
    {
      title: "limits.h",
      description: "The standard library containing constants for the minimum and maximum values of each type."
    },
    {
      title: "Architecture",
      description: "Type sizes (like long) change depending on whether you are on a 32 or 64-bit system."
    }
  ]} 
/>

## How big is an integer?

Unlike languages like Python, in C the size of an `int` is not fixed forever. It depends on your **platform**.

<LimitsExplorer />

<Callout type="warning" title="Watch the Warning!">
When using `sizeof` in `printf`, use the `%lu` (long unsigned) specifier or cast to `(int)`, otherwise the compiler will warn you that the types don't match!
</Callout>

## The Dance of Bits

Every number you write is converted into a sequence of **0**s and **1**s. A 32-bit `int` can represent over 4 billion different combinations.

<BinaryVisualizer />

<Infographic 
  src="/images/limits_table.png" 
  alt="C type limits table" 
  caption="Quick comparison between various integer types and their typical ranges."
/>

## Memory and Alignment

The computer doesn't just throw bytes randomly into RAM. It organizes them into cells, and often adds "padding" (empty space) to make access faster for the processor.

<Infographic 
  src="/images/memory_padding.png" 
  alt="Memory visualization and padding" 
  caption="How a mix of different types is stacked in RAM."
/>

## Code Lab

Try experimenting with `sizeof` and the `limits.h` constants like `INT_MIN` and `INT_MAX`.

<CodeEditor 
  initialCode={`#include <stdio.h>\n#include <limits.h>\n\nint main() {\n    printf("int size: %lu bytes\\n", sizeof(int));\n    printf("int range: %d to %d\\n", INT_MIN, INT_MAX);\n    return 0;\n}`}
  language="c"
/>

## Review Quiz

<Quiz 
  questions={[
    {
      question: "What does the sizeof operator return?",
      options: [
        "The maximum value of a variable",
        "The size in bits of a type",
        "The size in bytes of a type or variable",
        "The memory address of the variable"
      ],
      correctAnswer: 2,
      explanation: "sizeof always returns the number of bytes (8-bit groups) occupied in memory."
    },
    {
      question: "Which header file contains the INT_MIN and INT_MAX constants?",
      options: ["stdio.h", "limits.h", "stdint.h", "stdlib.h"],
      correctAnswer: 1,
      explanation: "limits.h defines architecture-specific limits for standard integer types."
    },
    {
      question: "On a 64-bit system, how large is a 'long' usually?",
      options: ["2 bytes", "4 bytes", "8 bytes", "16 bytes"],
      correctAnswer: 2,
      explanation: "On modern 64-bit systems, the long type usually follows the processor's word size, which is 8 bytes."
    }
  ]}
/>

---

<KeyConcepts title="Hacker Key Concepts">
  - **sizeof**: The only safe way to know how much space a type actually occupies on your machine.
  - **limits.h**: Contains constants (like `INT_MAX`) that describe the numeric limits of your compiler.
  - **Padding**: The computer adds empty space ("dead" bytes) between data to make access faster for the CPU.
</KeyConcepts>

### Hacker Challenge 🏆

<Quiz 
  title="Hacker Challenge: Final Quiz"
  questions={[
    {
      id: 1,
      question: "In C, what is the value of `sizeof('A')` on a machine where `int` is 4 bytes and `char` is 1 byte?",
      options: [
        "1, because 'A' is a character",
        "4, because in C, character literals (like 'A') are treated as `int`",
        "8, because it's a constant",
        "It depends on the font used"
      ],
      correctAnswer: 1,
      explanation: "This is a subtle difference between C and C++. In C, a character constant like 'A' has type `int`, so its size is that of an integer (usually 4 bytes). If you had declared `char c = 'A';` and checked `sizeof(c)`, the result would have been 1."
    },
    {
      id: 2,
      question: "Why does the compiler add 'padding' (empty space) between structure fields or variables in memory?",
      options: [
        "To waste memory and sell more RAM",
        "Because the CPU reads memory in blocks (words), and aligned data is much faster to read",
        "To logically separate data for aesthetic reasons",
        "It is a bug in old compilers"
      ],
      correctAnswer: 1,
      explanation: "Alignment is fundamental: a 64-bit CPU prefers to read data that starts at addresses that are multiples of 8 bytes. If a piece of data were 'straddling' two blocks, the CPU would have to perform two reads instead of one, halving performance."
    }
  ]}
/>

### Terminal Challenge: Header Hunting 🕵️

<HackerTerminal 
  title="Hacker Challenge: Level 4"
  description="Salvatore explained that to know the system limits (such as the maximum value of an integer), we must not guess, but use a standard header file. What is the name of this file?"
  command="ls /usr/include/..."
  expectedOutput="limits.h"
  inputLabel="Header File:"
  placeholder="e.g. stdio.h"
  hint="It deals with the numeric 'limits' of the system."
  explanation="Exactly! `limits.h` defines macros like `INT_MAX`, `SHRT_MIN`, etc. This allows your C code to be portable: if you compile it on a 16-bit machine, `INT_MAX` will be 32,767; on a 32-bit machine, it will be 2 billion."
/>

See you in the next lesson!
