import VideoEmbed from '../../components/content/VideoEmbed';
import Callout from '../../components/content/Callout';
import KeyConcepts from '../../components/content/KeyConcepts';
import CodeEditor from '../../components/exercises/CodeEditor';
import Quiz from '../../components/exercises/Quiz';
import ProTerminal from '../../components/content/ProTerminal';
import Infographic from '../../components/content/Infographic';
import ComparisonTable from '../../components/content/ComparisonTable';
import StackVisualizer from '../../components/content/StackVisualizer';
import MicroRegisters from '../../components/content/MicroRegisters';

# Lesson 2: Variables, Functions, and Memory

Welcome to the second lesson! Today we'll go deep into the structure of a C program, finally understanding what happens "under the hood" when we declare a variable or call a function.

<VideoEmbed videoId="Z84vlG1RRtg" title="Let's learn C: lesson 2" />

## Program Structure and `#include`

Every C program often starts with preprocessor directives. The most common one is `#include <stdio.h>`.

<Callout type="info" title="Standard I/O">
`stdio.h` stands for **Standard Input/Output**. It contains the definitions of fundamental functions like `printf`. Without this include, the compiler wouldn't know how to handle screen output.
</Callout>

### The main Function and C Constraints
The `main` function is the heart of the program. It is the entry point from which the operating system begins execution. In C, **all executable code must reside within a function**.

<Callout type="warning" title="No 'Free-Floating' Code">
Unlike Python or Bash, you cannot write a `printf` directly at the top of the file. If you try, the compiler will give you an error like `expected declaration or statement`.
</Callout>

```c
int main(void) {
    // Your code must stay inside here!
    return 0;
}
```

- `int`: Indicates that the function returns an integer to the operating system (0 means success).
- `void`: Explicitly indicates that the function takes no input arguments.

---

## printf: The Magic of Formatting

The name `printf` stands for **Print Formatted**. It is an extremely powerful function because it is **variadic**: it can accept a variable number of arguments.

<Callout type="danger" title="Variadic Dangers">
If you tell `printf` you will print two integers (`%d %d`) but only pass one, C will not stop you! It will read "garbage" from memory or crash. This is your first taste of **Undefined Behavior**.
</Callout>

```c
printf("Hello %s, result is %d\n", "User", 30);
//      [FORMAT]         [ARG 1]    [ARG 2]
```

---

## Functions: Prototype vs. Definition

Salvatore showed us how to define a function to sum two numbers. It's important to distinguish between how the function is "announced" and how it is "written."

<ComparisonTable 
  title="Function Anatomy"
  headers={["Element", "Example", "Description"]}
  rows={[
    ["Return Type", "int", "The type of data the function 'returns'."],
    ["Name", "sum", "The identifier used to call the function."],
    ["Parameters", "(int a, int b)", "The data the function receives as input."],
    ["Body", "{ return a + b; }", "The instructions enclosed in curly braces."]
  ]}
/>

---

## Variables and Explicit Types

In C, unlike languages like Python or JavaScript, you must **explicitly declare** the type of every variable. The compiler does not perform automatic "inference."

```c
int a = 10;   // Declaration and Initialization (assigns 10 during creation)
int b;        // Declaration only (contains RAM "garbage")
b = 20;       // Subsequent assignment (overwrites previous value)
int c = a + b; // Initialization via expression
```

<ComparisonTable 
  title="Declaration vs Initialization"
  headers={["Operation", "Example", "Effect in Memory"]}
  rows={[
    ["Declaration", "<code>int x;</code>", "Reserves space, but content is random."],
    ["Initialization", "<code>int x = 10;</code>", "Reserves space and writes the value immediately."],
    ["Assignment", "<code>x = 20;</code>", "Changes the value of an existing space."]
  ]}
/>

<Callout type="pro" title="Expert Mindset">
Specifying the type (e.g., `int`) allows the compiler to know exactly how many bytes to reserve in memory and how to interpret them. This is the secret to C's performance!
</Callout>

---

## The "Lifecycle" in Memory (The Stack)

What happens when you call a function? Where do local variables end up?
Variables defined inside a function live in a memory area called the **Stack**.

<StackVisualizer />

When a function is called, a **Stack Frame** is created. 

<Callout type="info" title="Creation and Destruction">
  Local variables (including function arguments) are created "on the fly" every time you call the function. When the function returns, the space is released. Note: "released" does not mean "deleted"! The data stays there until another function overwrites it, which is why uninitialized variables are dangerous.
</Callout>

When the function ends (`return`), its frame is "destroyed" and the memory is freed.

---

## Registers and Microprocessor (Appendix)

In this appendix, Salvatore takes us back in time with the **6502** microprocessor to explain the difference between registers and RAM.

<VideoEmbed videoId="r6mU_IHXEps" title="Appendix Lesson 2: The life of local variables" />

<MicroRegisters />

<Callout type="pro" title="Memory Mapped I/O">
  In the screen above, we are writing directly to RAM at address `$0200`. The microprocessor doesn't "know" it's drawing: it's just moving bytes. It's the video hardware that interprets those bytes as pixel colors. This is the foundation of embedded systems and old consoles.
</Callout>

## The `main` Return Value

Why does `main` return an `int`? It's used to tell the **Operating System** if the program finished successfully.
- `return 0;` means "Success".
- `return 1;` (or others) signals an error.

<Callout type="tip">
On Linux, you can see the return value of the last command by typing `echo $?` in the terminal.
</Callout>

### Return Codes and Shell Logic
The return value of `main` is not just a formality. In Unix-like systems, it allows chaining programs based on their success.
- **Logical AND (`&&`)**: `command1 && command2` -> `command2` is executed only if `command1` returns **0** (success).
- **Logical OR (`||`)**: `command1 || command2` -> `command2` is executed only if `command1` fails (non-zero code).

This "short-circuit" mechanism is fundamental for automating complex tasks where each step depends on the previous one.

<Callout type="tip">
**Registers** are tiny, lightning-fast "variables" inside the CPU. **RAM** is larger but slower. The C compiler tries to use registers as much as possible to speed up your code.
</Callout>

---

## Verification Exercises

<Quiz 
  title="Quiz Standard" 
  questions={[
    {
      id: 1,
      question: "What does the 'void' in int main(void) signify?",
      options: [
        "The function returns nothing",
        "The function accepts no parameters",
        "The program is empty",
        "The function is an error"
      ],
      correctAnswer: 1,
      explanation: "Using 'void' explicitly indicates that the main function receives no arguments."
    },
    {
      id: 2,
      question: "Where are a function's local variables stored?",
      options: [
        "In the Hard Disk",
        "In the Video Memory",
        "On the Stack",
        "In Global Registers"
      ],
      correctAnswer: 2,
      explanation: "Local variables live on the Stack and are removed when the function concludes."
    },
    {
      id: 3,
      question: "Which compiler flag enables all common warnings?",
      options: [
        "-Werror",
        "-O3",
        "-Wall",
        "-all"
      ],
      correctAnswer: 2,
      explanation: "-Wall (Warn All) is essential for catching potential bugs before running the program."
    },
    {
      id: 4,
      question: "What is the main difference between a Register and RAM?",
      options: [
        "Registers are slower",
        "RAM is inside the CPU",
        "Registers are inside the CPU and ultra-fast, RAM is external and larger",
        "There is no difference"
      ],
      correctAnswer: 2,
      explanation: "Registers are small memory units inside the CPU used for immediate operations. RAM is the system's main memory."
    },
    {
      id: 5,
      question: "Why do we initialize variables (e.g., int a = 0)?",
      options: [
        "Because C always requires it",
        "To prevent the variable from containing 'garbage' (old values left on the stack)",
        "To make the code longer",
        "To help the linker"
      ],
     }
  ]}
/>

---

## In Short

<KeyConcepts 
  title="In Short"
  concepts={[
    {
        title: "Explicit Typing",
        description: "In C, every variable has a fixed type. This ensures safety and total control over memory."
    },
    {
        title: "Stack Frames",
        description: "Function memory is organized like a stack: the last call is the first to be removed."
    },
    {
        title: "Compiler Warnings",
        description: "Always use -Wall. A 'complaining' compiler is a compiler that is helping you."
    },
    {
        title: "Registers vs RAM",
        description: "Registers are internal to the CPU and ultra-fast; RAM is external and larger."
    }
  ]}
/>

### Pro Challenge
<Quiz 
  title="Pro Challenge: Final Quiz"
  questions={[
    {
      id: 1,
      question: "Why is an uninitialized local variable (e.g., `int x;`) considered dangerous even if the program seems to work initially?",
      options: [
        "Because it occupies more RAM",
        "Because it contains 'garbage', which is the residual value from whoever used that piece of Stack previously (Stack Reuse)",
        "Because the compiler deletes the file if it finds it",
        "Because it makes the computer hotter"
      ],
      correctAnswer: 1,
      explanation: "The Stack is a memory area that is continuously reused. When a function finishes, its space is freed but the data is not wiped (for speed). The next function that uses that space will find the 'ghosts' of the previous function."
    },
    {
      id: 2,
      question: "What physically happens when we call a function in C?",
      options: [
        "The function's code is copied to the point of call",
        "A new 'Stack Frame' is created containing the function's parameters and local variables",
        "The CPU stops working until the function terminates",
        "Variables are saved to the hard disk"
      ],
      correctAnswer: 1,
      explanation: "A function call creates a structure in the Stack called a 'Frame'. It is like a temporary drawer where the function puts everything it needs. When the function 'returns', the drawer is emptied (or rather, removed from the stack)."
    },
    {
      id: 3,
      question: "Why do we sometimes use the `volatile` keyword before a variable?",
      options: [
        "To make it faster",
        "To tell the compiler not to optimize it (e.g., not put it in a register), because its value could change unexpectedly (e.g., external hardware)",
        "To delete it from memory after use",
        "To protect it from viruses"
      ],
      correctAnswer: 1,
      explanation: "The C compiler is very aggressive. If it sees you read a variable twice without writing anything in between, it might think the value is the same and not re-read it from RAM. `volatile` forces the compiler to re-read the value every single time, essential for interacting with hardware or threads."
    }
  ]}
/>

### Terminal Challenge: Investigating Memory 

<ProTerminal 
  challenges={[
    {
      id: "xxd-dump-en",
      title: "Seeing the Unseen",
      description: "Salvatore mentioned a tool to look 'inside' executables. Which command would you use to see the hexadecimal content of `a.out`?",
      commands: ["xxd a.out"],
      expectedOutput: "00000000:",
      hints: ["It starts with 'x' and performs a hex dump"],
      explanation: "Great! `xxd` is fundamental for seeing the actual bytes of a binary.",
      xpReward: 150
    },
    {
      id: "nm-symbols-en",
      title: "The Name List",
      description: "Every function becomes a 'symbol'. Which command lists all symbols (function and variable names) in `a.out`?",
      commands: ["nm a.out"],
      expectedOutput: "0000000000001000 T main",
      hints: ["Two-letter command, short for 'names'"],
      explanation: "Exactly! `nm` lets you see which functions are defined in your code.",
      xpReward: 200
    },
    {
      id: "objdump-header-en",
      title: "The Header",
      description: "Which command would you use to view the executable header and find its entry point?",
      commands: ["objdump -f a.out"],
      expectedOutput: "start address",
      hints: ["Starts with 'obj'"],
      explanation: "Perfect! `objdump -f` shows vital binary metadata.",
      xpReward: 250
    }
  ]}
/>

See you in the next lesson!


