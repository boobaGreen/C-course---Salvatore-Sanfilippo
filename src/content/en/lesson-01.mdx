import VideoEmbed from "../../components/content/VideoEmbed";
import Callout from "../../components/content/Callout";
import KeyConcepts from "../../components/content/KeyConcepts";
import CodeEditor from "../../components/exercises/CodeEditor";
import Quiz from "../../components/exercises/Quiz";
import ProTerminal from "../../components/content/ProTerminal";
import Infographic from "../../components/content/Infographic";
import ComparisonTable from "../../components/content/ComparisonTable";
import AssemblyExplorer from "../../components/content/AssemblyExplorer";
import TerminalSimulation from "../../components/content/TerminalSimulation";
import TypeMatcher from "../../components/games/TypeMatcher";

# Lesson 1: Introduction to C and "Hello World"

<VideoEmbed videoId="HjXBXBgfKyk" title="Introduction to C - antirez" />

Welcome! In this first lesson, we launch our journey through the C language. This course follows a very intuitive and practical style.

<Callout type="warning" title="Prerequisites">
  This course is designed for those who **already know how to program** in other
  languages (JavaScript, Python, Go, Ruby, etc.). Previous low-level experience
  is not required, but it's helpful to have an idea of how computers work under
  the hood.
</Callout>

The approach will be **extremely operational**: we'll learn to program by programming. Theoretical explanations will come as a "side effect" of writing the code.

<KeyConcepts title="What we'll learn today">
  - Writing your first "Hello World" program - Understanding the **compilation**
  process - Exploring what the compiler does (down to the Assembly level) - The
  democratization of C: from Stallman to GCC - Understanding **preprocessor**
  directives (#include) - Discovering **function prototypes**
</KeyConcepts>

<Callout type="info" title="Course Philosophy">
  This course follows an **instinctive** style. There is no obsessive
  preparation beforehand, but a freshness dictated by brutal practice. We will
  learn C as a "side effect" of solving real-world problems.
</Callout>

---

## "Hello World" in C

C is a very old language that has evolved relatively little in its basic syntax, but tremendously in its **compilers**. It remains a language of "few ideas" that must be deeply assimilated. Being a **compiled language**, we cannot execute the source code directly: we must first translate it into machine language.

Here is the classic starting program, in just 5 lines:

```c
#include <stdio.h>

int main() {
    printf("Hello, World!\n");
    return 0;
}
```

<Callout type="tip" title="Salvatore's Tip">
  Don't try to understand everything at once. In C, many concepts become clear
  only when we go a level deeper and see what happens in RAM or the CPU.
</Callout>

<TypeMatcher />

### How to compile and execute?

C is a **compiled** language. This means the code we write isn't read line-by-line by an interpreter (like Python or JS), but fully transformed into a binary file before it can be run.

<Infographic
  src="/images/c_compilation_pipeline_v2.png"
  alt="The Compilation Pipeline (Detailed)"
  caption="The 4 stages: Preprocessing, Compilation, Assembly, and Linking."
/>

We must use the **C compiler** (like `cc`, `gcc` or `clang`):

```bash
cc hello.c
```

### The Democratization of C

Historically, compilers were expensive and proprietary tools (like the **Intel C Compiler - ICC**). The turning point came with **Richard Stallman** and the **GCC** (GNU Compiler Collection) project. By gifting a powerful and free compiler to the world, Stallman allowed anyone to learn and use C, changing the history of free software forever.

This command generates a new executable file (typically named `a.out` on Unix systems). This file is a **binary** containing machine language and some operating system-specific headers (Linux, macOS, Windows). We can run it by typing:

<TerminalSimulation command="./a.out" output="Hello, World!" />

<CodeEditor
  initialCode={`#include <stdio.h>\n\nint main() {\n    printf("Hello, World!\\n");\n    return 0;\n}`}
/>

---

## Behind the Scenes: Assembly and Optimizations

We can ask the compiler to stop before generating the final binary, showing us the intermediate **Assembly** code using the `-S` flag.

This flag is fundamental because it allows us to inspect exactly how the compiler translated our C code, without having to "de-compile" a binary executable (which is much more complex).

Example usage:

```bash
cc -S hello.c
```

This command doesn't create an `a.out`, but instead generates a text file named `hello.s` containing the Assembly instructions.

Assembly is the "near-machine" language: it's readable by humans but corresponds 1:1 to the instructions the CPU will execute.

<AssemblyExplorer />

<Callout type="pro" title="Expert Mindset">
  Looking at Assembly isn't an academic exercise. It's the only way to *truly*
  understand how efficient your code is and what the compiler is doing behind
  your back.
</Callout>

As you saw in the explorer above, if we compile with the `-O2` flag (optimization level 2), we'll notice something surprising: **the `printf` function disappears**!

The compiler, realizing we are printing a simple string followed by a newline (`\n`), automatically replaces `printf` with the `puts` function, which is faster and more efficient for this specific purpose. This is just a "drop in the ocean" of the optimizations that a mature compiler can do.

---

## Breaking Down the Program

Let's analyze each line to understand what's really happening:

### 1. The Preprocessor and `#include`

```c
#include <stdio.h>
```

This line is not part of the C language itself, but a **preprocessor directive** (all directives start with `#`). Before actual compilation, the preprocessor takes the content of the `stdio.h` file (a standard library file) and "unpacks" it directly into our file.

Why do we need it? The C language itself has very few keywords (`return`, `int`, `while`). Functions like `printf` are not part of the language, but of the **Standard Library**. If we didn't include `stdio.h` (Standard Input/Output Header), the compiler wouldn't know what `printf` is!

<Callout type="info">
  C is a "naked" language. Without libraries, it doesn't even know how to print
  a character to the screen. Everything "useful" is found in the Standard
  Library.
</Callout>

<Callout type="warning" title="Security: printf vs puts">
  Never use `printf(buffer)` if `buffer` comes from user input! If the user
  enters strings like `%s%s%s`, it can cause a **Format String Attack**. `puts`
  is inherently safer because it does not interpret placeholders.
</Callout>

### 2. Header Files (`.h`) and Function Prototypes

In a header file (like `stdio.h`), there isn't the code that _executes_ `printf`, but there are **function prototypes**.

<ComparisonTable
  title="Prototype vs Definition"
  headers={["Feature", "Prototype (.h)", "Definition (.c)"]}
  rows={[
    [
      "Purpose",
      "Declares the existence and signature of the function",
      "Contains the actual code implementation",
    ],
    [
      "Syntax",
      "Ends with a semicolon: <br/><code>int sum(int a, int b);</code>",
      "Contains the body between braces: <br/><code>int sum(int a, int b) { ... }</code>",
    ],
    [
      "Visibility",
      "Allows other files to know how to call the function",
      "Compiled into the final binary object",
    ],
  ]}
/>

A prototype is similar to a function definition, but instead of having a block of code between curly braces `{}`, it ends immediately with a semicolon `;`:

```c
int printf(const char *format, ...);
```

A prototype tells the compiler: _"There exists a function called `printf` that accepts these parameters and returns an integer. Trust me, you'll find it later"_.

Thanks to the prototype, the compiler knows exactly how to prepare data in memory (Assembly) to call it correctly, even without knowing its internal code. In fact, we could even skip `#include <stdio.h>` and write the prototype by hand in our file to make the program work!

---

<Quiz
  title="Quiz Standard"
  questions={[
    {
      id: 1,
      question:
        "What happens when you compile with the -O2 flag if the code contains a simple printf with \\n?",
      options: [
        "A compilation error is generated",
        "The compiler replaces printf with puts to optimize performance",
        "The string is not printed",
        "The program becomes slower",
      ],
      correctAnswer: 1,
      explanation:
        "Modern compilers like GCC or Clang recognize simple patterns. puts is more efficient than printf for simple static strings.",
    },
    {
      id: 2,
      question: "What is a #include <stdio.h> directive for?",
      options: [
        "To define the main function",
        "To import standard library binary files",
        "To provide the compiler with Input/Output function prototypes",
        "To write the Assembly code for us",
      ],
      correctAnswer: 2,
      explanation:
        "The directive tells the preprocessor to include the header containing function prototypes (signatures).",
    },
    {
      id: 3,
      question: "What is the main difference between printf and puts?",
      options: [
        "puts automatically adds a newline (\\n), printf does not",
        "printf is faster than puts",
        "Only printf can print strings",
        "None, they are exactly the same",
      ],
      correctAnswer: 0,
      explanation:
        "puts prints the string and adds a newline at the end. printf prints exactly what is passed to it.",
    },
    {
      id: 4,
      question:
        'Why does the compiler turn printf("Hello\\n") into puts("Hello") but NOT printf("Score: %d\\n", 10)?',
      options: [
        "Because puts doesn't support numbers",
        "Because puts doesn't support format strings (like %d)",
        "Because printf is more modern",
        "Because there is no space in the executable",
      ],
      correctAnswer: 1,
      explanation:
        "puts only prints static strings. printf has a whole parsing engine to handle placeholders (%d, %s, etc.).",
    },
    {
      id: 5,
      question:
        "What happens if you forget the semicolon at the end of a prototype in a .h file?",
      options: [
        "The program crashes at runtime",
        "The preprocessor ignores the issue",
        "Compilation error: the compiler doesn't know where the signature ends",
        "The computer turns off",
      ],
      correctAnswer: 2,
      explanation:
        "The semicolon is mandatory in prototypes to separate declarations.",
    },
    {
      id: 6,
      question:
        "What is the value of an uninitialized local variable in C?",
      options: [
        "0",
        "A random value (garbage)",
        "Null",
        "Undefined",
      ],
      correctAnswer: 1,
      explanation: "In C, uninitialized local variables contain random values (garbage) left in that stack memory location."
    }
  ]}
/>

See you in the next lesson!

<KeyConcepts title="In Short">
  - Compiled, not executed: C is not executed, but **compiled**.
  - Smart optimization: The compiler can optimize code
  intelligently (e.g., turning `printf` into `puts`).
  - The Preprocessor: Prepares the file before compilation.
  - Header Files: **Header files (`.h`)** contain
  function **prototypes**.
</KeyConcepts>

### Pro Challenge
<Quiz
  title="Pro Challenge: Final Quiz"
  questions={[
    {
      id: 1,
      question:
        "Suppose you receive a string from a user and want to print it. Why is `puts(buffer)` safer than `printf(buffer)`?",
      options: [
        "puts is faster",
        "printf would interpret any '%' symbols in the string as placeholders, trying to read random data from the stack (Format String Attack)",
        "puts automatically encrypts the string",
        "There is no security difference",
      ],
      correctAnswer: 1,
      explanation:
        "This is a classic security attack. If a user enters '%s%s%s', printf will try to read strings from memory addresses pointed to by random values on the stack, causing a crash or data theft. puts, however, prints the string 'as is', ignoring special symbols.",
    },

    {
      id: 2,
      question: "What is the role of the C compiler relative to Assembly?",
      options: [
        "The compiler writes Assembly code that then must be manually translated",
        "The compiler automates the translation from C to Assembly (and then to binary), applying optimizations that a human would have difficulty doing by hand",
        "There is no link, they are separate languages",
        "Assembly is slower than C",
      ],
      correctAnswer: 1,
      explanation:
        "The compiler is an expert translator. It takes your C code and 'lowers' it to the machine level (Assembly), while trying to eliminate useless steps to make the program as fast as possible.",
    },
    {
      id: 3,
      question:
        "What happens if you compile a program with the `-static` flag?",
      options: [
        "The program can no longer be modified",
        "All necessary libraries are included inside the executable, making it larger but portable to systems without those libraries",
        "The program becomes slower because it doesn't use the cache",
        "All screen prints are removed",
      ],
      correctAnswer: 1,
      explanation:
        "Static linking copies the code of library functions (like printf) directly into your binary. This makes it independent of the versions of libraries installed on the system, but significantly increases its size.",
    },
  ]}
/>

### Terminal Challenge: The Optimization Trap 

<ProTerminal
  challenges={[
    {
      id: "opt-trap-en",
      title: "The Subtle Optimization",
      description:
        "You compiled a program that prints 'Hello World' with `printf`. If you look at the assembly generated with `-O2`, you will notice that the compiler has replaced the original call with a faster function for handling constant strings. Use `grep` to search for the line that calls a function (contains 'call') inside the assembly file `hello.s`.",
      commands: [],
      expectedOutput: "",
      simulatedSuccessOutput: `        call    puts@PLT`,
      expectedCommand: "grep 'call' hello.s",
      hints: [
        "The compiler 'downgrades' the function if there's no formatting",
      ],
      explanation:
        "Exactly! The compiler replaces printf with puts for simple static strings because it's much more efficient.",
      xpReward: 150,
    },

    {
      id: "elf-check-en",
      title: "Binary Anatomy",
      description:
        "A Linux executable isn't just code. Which command identifies the file structure and the architecture it was created for?",
      commands: [],
      expectedOutput: "",
      simulatedSuccessOutput: "hello.out: ELF 64-bit LSB executable, x86-64, version 1 (SYSV)",
      expectedCommand: "file a.out",
      hints: ["Look for a command that means 'document' or 'record'"],
      explanation:
        "Great! The `file` command reads the ELF header to determine the binary type.",
      xpReward: 200,
    },
    {
      id: "strip-symbols-en",
      title: "Final Cleanup",
      description:
        "Which command removes the symbol table and debug info to reduce the file size?",
      commands: [],
      expectedOutput: "",
      simulatedSuccessOutput: "",
      expectedCommand: "strip a.out",
      hints: ["It means to 'peel off' or 'remove'"],
      explanation:
        "Perfect! `strip` makes the binary smaller and harder to analyze with reverse engineering.",
      xpReward: 250,
    },
  ]}
/>

See you in the next lesson!


