import VideoEmbed from '../../components/content/VideoEmbed';
import Callout from '../../components/content/Callout';
import KeyConcepts from '../../components/content/KeyConcepts';
import RecursionVisualizer from '../../components/content/RecursionVisualizer';
import SwitchBoard from '../../components/content/SwitchBoard';
import CodeEditor from '../../components/exercises/CodeEditor';
import Quiz from '../../components/exercises/Quiz';

# Lesson 7: Loops, Recursion & Switch

Sometimes we need to repeat an operation thousands of times, or choose between many different options in an elegant way. Today we explore loops and the power of recursion.

<VideoEmbed videoId="HCRthhjbfAg" title="Lesson 7: Loops and Recursion" />

<KeyConcepts 
  concepts={[
    {
      title: "Loops (for, while, do-while)",
      description: "Three different ways to repeat code. 'for' is ideal when you know how many turns to take, 'while' when you are waiting for a condition."
    },
    {
      title: "Recursion",
      description: "A function that calls itself. It's like a Russian doll (matryoshka) of executions accumulating on the stack."
    },
    {
      title: "Switch",
      description: "A clean alternative to many chained 'if-else' statements. It works like a fast jump table."
    }
  ]} 
/>

## Loops: for vs while
As Salvatore explains, a `for` loop is often just a more compact way of writing a `while`.

```c
// WHILE Version
int i = 0;
while (i < 5) {
    // code
    i++;
}

// FOR Version (cleaner)
for (int i = 0; i < 5; i++) {
    // code
}
```

We use `for` when we already know how many times to iterate, and `while` when the condition depends on something that will happen in the code.

## Recursion and the Stack

When a function calls itself, the computer doesn't start from scratch. It "freezes" the current function and opens a new one on top of it.

<RecursionVisualizer />

<Callout type="warning" title="Stack Overflow">
If recursion has no "exit condition" (base case), it will keep creating new frames until it runs out of memory dedicated to the stack. This causes the famous 'Stack Overflow' crash.
</Callout>

## Choosing Elegantly: The Switch

If you have a variable that can be 1, 2, 3... instead of writing ten `if`s, we use `switch`. It's more readable and often faster!

<SwitchBoard />

<Callout type="info" title="The Importance of Break">
In a `switch`, if you forget the `break;`, the code will continue to execute subsequent cases as well! This behavior is called 'fall-through'.
</Callout>

## Loops Lab

Try transforming this `while` loop into a `for` loop.

<CodeEditor 
  initialCode={`#include <stdio.h>\n\nint main() {\n    int i = 0;\n    while (i < 5) {\n        printf("Turn number %d\\n", i);\n        i++;\n    }\n    return 0;\n}`}
  language="c"
/>

## Review Quiz

<Quiz 
  questions={[
    {
      question: "Which of these loops guarantees AT LEAST one execution of the code inside?",
      options: ["for", "while", "do-while", "none"],
      correctAnswer: 2,
      explanation: "The do-while loop checks the condition at the END, so it runs the body at least once."
    },
    {
      question: "What happens if you forget 'break' in a switch case?",
      options: [
        "A compilation error is generated",
        "Execution stops",
        "Execution continues into the next case (fall-through)",
        "The computer restarts"
      ],
      correctAnswer: 2,
      explanation: "Without break, C continues to execute instructions in the next case until it hits one or the switch ends."
    },
    {
      question: "Why is recursion useful?",
      options: [
        "It's always faster than loops",
        "It allows solving complex problems by dividing them into simpler sub-problems",
        "It uses no memory",
        "It's mandatory in C"
      ],
      correctAnswer: 1,
      explanation: "Recursion is ideal for recursive data structures (like trees) and for the 'divide and conquer' approach."
    }
  ]}
/>
